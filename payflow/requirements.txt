# Install: pip install -r requirements.txt
# Editable install from repo root: pip install -e ".[dev]"  (see pyproject.toml)

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
aiosqlite>=0.20.0
pydantic-settings>=2.6.0
loguru>=0.7.0
sqlalchemy>=2.0.0
alembic>=1.14.0
apscheduler>=3.10.0
